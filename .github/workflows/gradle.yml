name: Dev Builds

on:
  push:
    branches: [ '**' ] # stop build tag commit
    paths:
      - "*.gradle"
      - "gradle.properties"
      - "src/**"
      - "versions/**"
      - ".github/**"
  pull_request:

env:
  IS_THIS_RELEASE: false

jobs:
  build:
    uses: ./.github/workflows/build.yml
